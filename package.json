{
  "name": "ferrisscript",
  "version": "0.0.2",
  "description": "FerrisScript - A Rust-powered scripting language for Godot",
  "private": true,
  "scripts": {
    "docs:lint": "markdownlint \"**/*.md\" --ignore node_modules --ignore target --dot",
    "docs:links": "echo \"Use PowerShell script for link checking: .\\scripts\\lint-docs.ps1\"",
    "docs:lint:fix": "markdownlint \"**/*.md\" --ignore node_modules --ignore target --dot --fix",
    "docs:fix": "npm run docs:lint:fix"
  },
  "devDependencies": {
    "markdown-link-check": "^3.12.2",
    "markdownlint-cli": "^0.41.0"
  }
}
